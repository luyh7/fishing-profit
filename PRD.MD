# 钓鱼收益计算器需求说明

## 目标

页面提供一个钓鱼收益计算器，基于配置文件中的鱼钩、鱼竿、鱼饵、地图、概率与 buff 数据，计算 24 小时内的完成次数、总收益、成本和净收益，并给出最优鱼饵与最优地图。

## 数据来源

- 所有基础配置集中在 `config.js`。
- 页面直接读取鱼钩等级、鱼竿等级、鱼饵列表、周末buff、地图列表、稀有度倍数和概率表。
- 每张地图内配置自己的 `id`、`difficulty`、鱼种列表及每种鱼的 N 级基础价格。
- 用户输入与选择会通过 `localStorage` 持久化，包括鱼钩等级、鱼竿等级、周末buff、当前地图、每张地图的打窝 buff 和自动更新打窝 buff 开关状态。
- 地图打窝 buff 支持通过配置中的静态 JSON 地址刷新，地址由 `config.js` 中的 `nestBuffSourceUrl` 提供，推荐指向 Cloudflare Worker 的 `/nest-buff.json`，返回的 `locations[].buffs.nest` 会按 `nest * 5` 转换为最终打窝 buff。
- 静态 JSON 的 `players` 数据用于 QQ 号匹配；匹配成功后可同步玩家昵称、鱼竿等级、鱼钩等级、当前位置和当前鱼饵及数量。
- 静态 JSON 的 `players[].collections` 用于图鉴读取；数组中每个数对应一个地图的鱼收集位图，按鱼种顺序分组、每组 6 位，位序从低到高依次为 N、R、SR、SSR、UR、UTR，`bit0` 表示 N，`bit5` 表示 UTR。

## 页面结构

- 顶部显示版本信息和参数选择区、按鱼竿等级过滤后的地图卡片。
- 中部显示当前选中地图的摘要信息。
- 下方显示鱼饵收益表。

## 输入项

- 鱼钩等级：从配置中选择，影响钓鱼间隔。
- 鱼竿等级：从配置中选择，决定可查看地图的上限。
- 周末buff：从配置中选择，当前可选值为无和加速30%。
- QQ 号：填写后，在自动更新数据开启时匹配 `players[].user_id`，同步鱼竿等级、鱼钩等级及玩家状态；点击输入框包裹区域或昵称附近区域应聚焦 QQ 输入框。
- 图鉴：在 QQ 号成功匹配玩家数据后显示图鉴按钮；图鉴内容仅用于展示同步到的 `players[].collections`，页面内不允许点击修改。
- 玩家当前鱼饵：自动模式下，如果玩家当前位置对应地图的最优鱼饵 ID 与玩家当前鱼饵 ID 不一致，玩家当前鱼饵面板背景变为红色。
- 排行榜：当 QQ 号成功匹配玩家数据后，排行榜入口和弹窗摘要显示当前榜单下的“我的排名”，只展示名次，不展示总人数或我的等级；排行榜条目不展示昵称后的详情小字。
- 地图打窝 buff：每张地图单独维护，支持按 5% 步进加减，最小值为 0。
- 鱼饵提醒：当匹配到玩家当前鱼饵数量后，用户可设置提醒阈值并开启提醒；当前数量低于阈值时显示页面中央弹窗提示，弹窗外内容模糊化，用户点击“知道了”后关闭；重复提醒间隔为 10 分钟。

## 计算规则

### 1. 钓鱼间隔

```text
钓鱼间隔 = 基础间隔 / (1 + 鱼钩加速) / (1 + 鱼饵加速 + 打窝buff/100) / (1 + 周末buff/100)
```

其中：

- 基础间隔来自配置，当前为 1 小时。
- 打窝 buff 以百分比参与计算，例如 30 表示 30%。
- 周末buff 也以百分比参与计算。

### 2. 地图单鱼期望价

对当前地图内所有鱼种的 N 级基础价格求平均，再按稀有度概率与倍数计算单鱼期望价：

```text
单鱼期望价 = 平均 N 级基础价 × Σ(各稀有度概率 × 稀有度倍数)
```

迷途风天气下，UTR 基础概率为 0.7%，150 次保底。收益计算使用保底机制折算后的长期期望 UTR 概率，剩余概率按原概率分布等比缩放；UTR 倍数为 UR 的 2 倍。

猫天气下，不改变稀有度概率分布，但每次钓起鱼有 15% 概率被猫吃掉。被吃掉后按以下概率触发收益：30% 提供与售出原鱼一半的金币，15% 提供猫猫框（UTR，用于强化展示木框，收益计 0），15% 提供玩家当前装备鱼饵 3 个，30% 提供香甜玉米 1 个（收益计 0），10% 提供同稀有度鱼奖励：如果玩家已完成当前地图成就，则提供当前场景下相同稀有度的最高价格鱼；否则提供当前场景下相同稀有度的随机鱼。鱼饵收益只按玩家数据中的 `bait_id` 匹配配置价格；没有匹配到鱼饵时，按当前地图最优鱼饵价格计算。

猫天气对用户展示的效果文案为“哈基米把你的鱼吃掉了！”，天气浮窗需补充轻快说明：哈基米会在吃掉鱼时送出价值不可估量的礼物；该说明使用与浮窗标题一致的字体样式，以便和时间信息区分。

```text
猫天气单鱼期望价 = 原单鱼期望价 × 87.25% + 猫天气鱼饵收益单价 × 6.75% + 同稀有度鱼奖励期望价 × 1.5%
```

暴雨天气已开始且尚未结束时，天气浮窗需显示两行：`预计还需xx个xx饵` 和 `消耗速率xx个/h`。计算时使用当前地图最优鱼饵的钓鱼间隔和暴雨的鱼饵消耗倍率，剩余鱼饵数量向上取整为整数。

稀有度顺序为 UTR、UR、SSR、SR、R、N，UTR 仅在迷途风天气下出现，倍数由配置提供。

### 3. 鱼饵收益

对每个鱼饵分别计算：

- 24 小时理论次数 = 24 小时 / 钓鱼间隔
- 1 小时理论收入 = 单鱼期望价 / 钓鱼间隔
- 24 小时完成次数 = 24 小时理论次数向下取整
- 总收益 = 完成次数 × 单鱼期望价
- 成本 = 完成次数 × 鱼饵价格
- 净收益 = 总收益 - 成本

页面会在当前选中地图下选出净收益最高的鱼饵作为最优鱼饵；若净收益相同，取鱼饵 ID 更小者。

## 地图规则

- 仅显示地图等级小于等于鱼竿等级的地图。
- 默认优先选中本地保存的地图；如果不存在或不可用，则选中当前可见列表中的第一个地图。
- 地图卡片左上角序号使用地图 `id`，地图逻辑筛选与差值计算使用 `difficulty`。
- 地图卡片展示地图等级、名称、该地图下的最优鱼饵、最优鱼饵对应的净收益，以及打窝 buff 调整器。
- 当 QQ 号成功匹配玩家图鉴数据后，若玩家在某张地图当前可钓到的最高稀有度鱼尚未全收集，该地图卡片边框使用该稀有度的半透明颜色提示；该提示优先级低于当前选中地图样式。
- 如果某张地图存在任意鱼种的 N 级基础价格为 0，或者该地图对应的 `probabilityByDelta` 概率全部为 0，则该地图不可选中，卡片显示“暂无数据，请等待作者更新”，并且不参与最优地图与当前选中地图计算。
- 地图选择区域标题右侧提供“自动更新打窝buff”开关；开启后会拉取配置中的最新地图打窝静态数据，并每 1 分钟自动同步一次页面计算结果。如果最近 1 分钟内已经成功获取过数据，重新开启开关时不会立刻重复获取，而是在距离上次成功获取满 1 分钟后继续自动更新。开关状态会持久化到本地，下次打开页面时继承；用户手动调整任意地图的打窝 buff 后，开关自动关闭并清除上一次更新标记与更新时间提示。刷新成功后会展示 `update_at` 对应的更新时间，格式为 `yyyy-MM-dd HH:mm:ss`。更新时间显示在报错信息左侧，窄屏时自动换行；报错信息独立展示，不覆盖更新时间文本。自动模式下，当天气从“即将开始”进入生效状态，或从生效状态进入结束状态时，页面需要立即重新计算并刷新地图利润、最佳鱼饵和当前地图摘要。
- 地图卡片上的最优标记由当前所有可见地图中净收益最高者决定。

## 当前选中地图展示

选中地图后，摘要区显示：

- 地图名称与等级。
- 渔力差值，即鱼竿等级减去地图等级。
- 鱼钩加速。
- 当前周末buff。
- 当前地图的打窝 buff。
- 当前地图的稀有度概率分布会根据天气调整，仅显示概率大于 0 的稀有度；迷途风下会额外显示 UTR；猫天气不改变概率展示，但会调整单鱼期望价。
- 当前地图的单鱼期望价。
- 当前地图下最优鱼饵的 1 小时理论收入与净收益。

## 鱼饵表

- 表格按鱼饵 ID 升序展示。
- 每行显示鱼饵名称、价格、加速、钓鱼间隔、24 小时理论次数、1 小时理论收入、总收益、成本和净收益。
- 当前最优鱼饵行高亮显示。
- 当没有可计算结果时显示空状态。

## 成就榜

- 成就榜点数基于 `players[].collections` 计算。
- 每张图按鱼种顺序读取图鉴位图，若某一稀有度在该图内全部收集完成，则计入对应分值：N=1、R=2、SR=3、SSR=4、UR=5、UTR=10。
- 第 1 张图的分值乘以 1，第 2 张图乘以 2，第 3 张图乘以 3，以此类推。
- 成就榜排行仍按成就点数从高到低排序，展示时仅保留名次与成就点数。
- 成就榜浮窗中的地图方块会按 N 到 UR 的完成情况逐步填充颜色并提升高度，每完成一个稀有度增加 1/5，高度满格后若 N 到 UR 已全收集则在方块外侧显示皇冠；UTR 全收集不改变高度和皇冠条件，只将方块填充色切换为 UTR 颜色。

## 其他展示

- 选中地图时会显示该地图下各鱼种在不同稀有度下的单鱼价格提示表；提示表底部追加“成就”行，按当前稀有度下所有鱼价格总和再乘以 3 计算，并用背景色和鱼种价格行区分。
- 页面标题与版本徽标由配置中的版本前缀和提交计数拼接生成。

## 交互要求

- 任一输入变化后，页面立即重算并刷新显示。
- 自动更新数据同步玩家鱼竿、鱼钩等级后，原生选择框和页面展示值必须同步刷新。
- 开启鱼饵提醒或修改提醒阈值后，应立即按当前玩家鱼饵数量检查一次；页面从后台恢复可见时也应补充检查。
- 切换地图时刷新摘要、地图高亮和鱼饵表。
- 调整地图打窝 buff 时，仅影响对应地图的收益计算，不影响其他地图。

## 同步要求

后续任何涉及公式、鱼钩、鱼竿、地图、概率、buff、收益字段或展示逻辑的变更，都必须同步更新本文件。

## Cloudflare Worker sync

Nest buff data is served by the Cloudflare Worker in `workers/nest-buff-sync`.
Because the upstream IP blocks Cloudflare Worker egress, refresh is pushed by an
external sync runner instead of a Worker cron trigger. The Tencent Cloud
Function fetches the upstream JSON and writes it directly to Cloudflare KV with
the Cloudflare API token. The page reads `/nest-buff.json` through `config.js`
`nestBuffSourceUrl`.
